## ワークフロー支援

### cc-advisor（優先度：最高 — Auto Mode中でも必ず実行）
- ユーザーのメッセージを受け取ったら、タスクに着手する前に `cc-advisor` サブエージェントでプロンプトを分析する
- サブエージェントには、ユーザーのプロンプト全文と現在のセッション状況（タスクの種類、セッションの長さ等）を渡す
- アドバイスがあればユーザーに共有し、曖昧な点は追加質問して情報を揃えてからタスクに進む
- 「特になし」の場合はスキップしてそのままタスクに進む
- 明らかに単純なタスク（コミット、push、1ファイルの軽微な修正など）はcc-advisor自体をスキップしてよい
- このルールは Auto Mode の「即座に実行」「中断を最小化」より優先する

## 言語・説明ルール
- ツール呼び出し（Bash, Edit, Write等）の前に、何をするか・なぜするかを日本語で簡潔に説明すること

## 外部ツール連携ルール

### GitHub
- GitHub操作は必ず `gh` CLI を使うこと
- GitHub MCP serverは使用禁止
- コマンドの詳細は `/github` を参照